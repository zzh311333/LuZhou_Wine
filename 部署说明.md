# 🚀 泸州老窖科普平台 · 公网部署说明

本平台采用 **纯静态部署**（火山引擎 IGA Pages）：公网上是一个**只读的网站快照**，
后台管理仍在你自己的电脑上使用（数据保存在本地 SQLite）。

## 两种使用方式

| 场景 | 访问地址 | 说明 |
|:--|:--|:--|
| 本地开发 | `http://localhost:5173`（需先启动后端 `node server/index.js`） | 后台编辑实时可见 |
| 公网静态站 | `https://xxxx.igapages.com`（部署后获得的链接） | 访客浏览/搜索/答题，内容为发布时的快照 |

> 前台内容会自动选择数据源：本地有后端就走实时数据，公网没后端就自动使用打包好的内容。

---

## 〇、当前公网地址（2026-08-07 部署）

```
https://lzh-wine-n5886g5jpm-l5u4le2rkw.preview.iga-pages.com?iga_token=288b748c6143ee3b9c87a557405c5a93&iga_time=1786205307
```

> ⚠️ 这是**预览链接**，`?iga_token=...` 参数可能随时间过期。
> 打不开时，在项目根目录运行 `iga pages preview` 获取新的完整链接（带新参数）。
> 若要永久地址（自定义域名），请在[火山引擎控制台](https://console.volcengine.com/dcdn/pages/)的项目设置里配置。

---

## 一、第一次部署（一次性准备）

### 1. 安装部署工具
在**项目根目录** `e:\tempery\LuZhou_Wine\LZH_WINE` 打开终端，运行：

```bash
npm i -g @iga-pages/cli@latest
```

验证安装：`iga --version`（需要 ≥ 1.0.5）。

### 2. 登录（只需一次，会记住登录状态）
```bash
iga login
```
会弹出浏览器，登录火山引擎账号完成授权即可。

### 3. 部署上线
```bash
iga pages deploy --name lzh-wine
```
- 第一次会自动创建项目并上传
- 结束后输出一行**公网链接**（形如 `https://xxx.igapages.com`）
- ⚠️ 如果链接带有 `?iga_token=...&iga_time=...`，把**完整链接**发给别人，不要省略参数

打开链接检查：首页 / 分类 / 文章 / 搜索 / 问答都应能正常浏览。

---

## 二、以后更新内容（三步发布）

在本地后台改完内容后：

1. **导出**本地数据库改动回内容源文件：
   ```bash
   node server/export.js
   ```
   （或一键：`npm run publish`，它等于「导出 + 构建」两步）

2. **构建**：
   ```bash
   npm run build
   ```
   （`npm run publish` 已包含此步）

3. **重新部署**：
   ```bash
   iga pages deploy
   ```
   公网站点即更新为最新内容。

---

## 三、本地管理内容

- 启动后端：`cd server && npm run dev`（或 `node index.js`）
- 启动前端：项目根目录 `npm run dev`
- 打开 `http://localhost:5173/admin` 登录后台（默认账号 **admin**，登录后请及时修改密码）
- 后台改动保存到本地数据库 `server/data.db`；**发布到公网前记得先 `node server/export.js`**

### 直接在源码里新增内容（不经过后台）

如果你像写文章一样直接改 `src/content/articles/*.md` 或 `src/content/quiz.js`，需要先把这些新内容**同步进数据库**，否则 `export.js` 重建时会当作"已删除"而清掉它们：

```bash
node server/import.js   # 把 src/content/ 里"新增"的文章/题目写进数据库（已存在的不覆盖）
node server/export.js   # 再从数据库重建规范的内容文件（可选，发布前执行）
```

> 简言之：**改源码文件 → import 进库 → export 回写**，两个数据源就一致了。

---

## 常见问题

- **公网上找不到后台/登录不了？** 属正常。公网是只读快照，后台只在本机使用。
- **公网上文章内容还是旧的？** 需要按上面「三步发布」导出 → 构建 → 重新部署。
- **部署命令报错？** 确认在项目根目录执行、已 `iga login`、CLI 版本 ≥ 1.0.5。
